apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: projectionv2

resources:
  - ../base
  - namespace.yaml
  - ingress.yaml
  - ingress-redirect.yaml
  - middleware.yaml

replicas:
  - name: projection-web
    count: 2
