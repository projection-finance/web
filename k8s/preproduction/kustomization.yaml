apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: projectionv2-preprod

resources:
  - ../base
  - namespace.yaml
  - ingress.yaml

replicas:
  - name: projection-web
    count: 1
